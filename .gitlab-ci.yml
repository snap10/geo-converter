stages:
  - deploy

pages:
  stage: deploy
  image: node:18
  script:
    - yarn install
    - yarn build
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
